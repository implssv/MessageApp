<%= simple_form_for([@message, @message.comments.build]) do |f| %>

    <%= md_simple_editor  do %>
        <%= f.input :content, as: :text, labell: "Comment"  %>
    <% end %>

    <%= f.button :submit %>
<% end %>